# ClaudeForge Configuration
CLAUDEFORGE_CONFIG=./config.json
CLAUDEFORGE_PORT=3000
CLAUDEFORGE_WEB_PORT=8080
CLAUDEFORGE_DEFAULT_PERMISSIONS=allow

# GitHub Server (if using)
GITHUB_PERSONAL_ACCESS_TOKEN=your-github-token-here

# PostgreSQL Server (if using)
DATABASE_URL=postgresql://localhost/mydb

# Other MCP server environment variables
# Add any environment variables needed by your MCP servers here